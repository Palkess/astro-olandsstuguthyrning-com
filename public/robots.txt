# Crawling is allowed everywhere. Any non-live build is kept out of search by a
# `noindex` meta tag instead (src/layouts/Layout.astro) — blocking crawling here
# would NOT prevent indexing, it would only stop Google reading the noindex.
User-agent: *
Disallow:

Sitemap: https://olandsstuguthyrning.com/sitemap-index.xml
